services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ticket_db}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      - ticket_analyzer

  backend:
    image: ${DOCKERHUB_USERNAME:-local}/ticket-analyzer-backend:${IMAGE_TAG:-dev}
    build:
      context: ./backend
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://postgres:postgres@db:5432/ticket_db}
      MODEL_NAME: ${MODEL_NAME:-distilbert-base-uncased-finetuned-sst-2-english}
      SENTIMENT_MODEL: ${SENTIMENT_MODEL:-distilbert/distilbert-base-uncased-finetuned-sst-2-english}
      HF_HOME: ${HF_HOME:-/app/.cache/huggingface}
      TRANSFORMERS_OFFLINE: ${TRANSFORMERS_OFFLINE:-1}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - ticket_analyzer

  frontend:
    image: ${DOCKERHUB_USERNAME:-local}/ticket-analyzer-frontend:${IMAGE_TAG:-dev}
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    restart: unless-stopped
    networks:
      - ticket_analyzer

networks:
  ticket_analyzer:
    name: ticket_analyzer

volumes:
  postgres_data:
